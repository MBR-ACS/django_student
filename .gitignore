# Python bytecode files
*.pyc
*.pyo
__pycache__/

# Django migrations (optional, if you don’t want to track them during early dev)
*/migrations/*.py
!*/migrations/__init__.py
# If you’re working in a team, usually migrations should be committed so that everyone’s DB schema stays in sync. 
# Ignoring them may cause inconsistencies across environments.

# Virtual environment
venv/
env/

# SQLite database (if using local dev DB)
# db.sqlite3

# Media uploads (don’t commit user-uploaded files)
media/

# Static files (collected using collectstatic)
staticfiles/

# Environment variables / secrets
.env
*.env

# IDE / editor settings
.vscode/
.idea/
*.swp
